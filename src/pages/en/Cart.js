import React from "react";
import PropTypes from "prop-types";
import Head from "@/app/head";

const cart = (props) => {
  return (
    <div>
      <Head title={"cart"} />
      <h1>cart</h1>
    </div>
  );
};

cart.propTypes = {};

export default cart;
